---
# to install, run ./setup.sh
# or ansible-galaxy install -r requirements.yml
collections:
  - name: community.aws
    version: 1.5.0
  - name: amazon.aws
    version: 1.5.0
  - name: performancecopilot.metrics
    version: 2.2.4
